# Текущее состояние

> Обновлено: 2026-08-25

Здесь только то, что происходит с проектом сейчас. Истории коммитов, разбора
законченных эпизодов и пожеланий владельца тут быть не должно — см. `git log`,
`docs/records/` и `docs/backlog.md`.

## Чем занимаемся

Идёт переработка пакета до версии 1.2.0 по плану
`docs/records/2026-08-25[5]-package-rework-plan.md`.

**Волна 0 (тестовый фундамент) и волна 1 (четыре критических дефекта)
закончены.** Отчёты — `2026-08-25[7]-wave-0-tests-report.md` и
`2026-08-25[9]-wave-1-critical-report.md`.

Что есть под руками у любой правки в `lib/`:

| Файл | Что делает |
|:--|:--|
| `test/support/reference.dart` | точная рациональная арифметика на `BigInt` и детерминированный генератор |
| `test/support/expect.dart` | хелперы проверки для обоих семейств |
| `test/property_test.dart` | 13 законов против модели вместо проверки отдельных значений |
| `test/competitors_test.dart` | сверка каждой операции с пакетом `decimal` плюс список осознанных расхождений |
| `test/defects_test.dart` | воспроизводители дефектов; закрытые — без `skip` |
| `test/decimal/`, `test/short_decimal/` | по файлу на тему, раскладка — в `docs/conventions.md` |

Решения, принятые по ходу и не подлежащие пересмотру вслепую:

- **Красных тестов в `main` нет.** Воспроизводитель дефекта пишется с
  **корректным** ожиданием и помечается `skip: 'ДN: …'`. **Снятие `skip` —
  критерий готовности каждой правки**, и его видно в диффе.
- **Эталонная модель не использует код пакета.** Иначе она повторяла бы его
  ошибки вместо того, чтобы их ловить.
- **Генератор детерминирован**, своё зерно на каждый тест; номер случая и
  значения печатаются в `reason`, поэтому падение воспроизводится сразу.
- **Представление сверяется только там, где оно и есть предмет проверки** —
  `parse`, `toString`, `toStringAsFixed`, `optimize`. В арифметике `base` и
  `scale` не сверяются: оптимизация законно даёт другую форму при том же
  значении. **Блокировка волны 3 этим снята.**
- **Знак делителя выносится до разложения, а отрицается результат, не
  делимое.** У `ShortDecimal` отрицание делимого на `int.min` молча теряет
  знак.
- **`_align` в сравнениях больше не участвует** — там `_compare`, который не
  переполняется. `_align` остался у `~/`, `%`, `remainder`,
  `divideToFraction` и `ShortDivision`: там переполнение настоящее, а не
  промежуточное, и по решению владельца остаётся молчаливым.

## Следующий шаг

**Волна 2 — одиннадцать оставшихся дефектов** (Д5–Д15). Взята в работу, план
правок — `docs/records/2026-08-26[1]-wave-2-defects-plan.md`; там же разобраны
два воспроизводителя из волны 0, чьи ожидания противоречат друг другу и будут
переписаны. Готовность каждого
видна сразу: тест в `test/defects_test.dart` со снятым `skip`.

Ближайший по соседству — Д5 (`toStringAsFixed` не добивает нулями у значений
меньше единицы): он единственный, кто держит `skip` на property-тесте.

Второй критерий готовности волны 2 — покрытие не ниже 90 % по каждому файлу
`lib/`. Сейчас провал в дробях и делении с остатком, см. «Известные пробелы».

Открытых вопросов к владельцу нет.

## Решения владельца, действующие сейчас

Обоснование каждого — в `docs/records/2026-08-25[4]-package-rework-design.md`.

1. **Две ступени: 1.2.0, затем 2.0.** Все пятнадцать дефектов чинятся без слома
   API, недостающее добавляется аддитивно. Ломающее — отдельно в 2.0.
2. **Переполнение `ShortDecimal` остаётся молчаливым.** Чинятся только случаи,
   где переполняется промежуточное значение при том, что аргументы и
   канонический результат в int64 укладываются.
3. **Общий интерфейс `FixedPoint` плюс тест симметрии**, реализации остаются
   раздельными. В 1.2.0 интерфейс внутренний, не экспортируется. Имя выбрано с
   расчётом на то, что когда-нибудь он может стать публичным супертипом в роли
   `num` для `int` и `double`. Межсемейных операций не будет: смешивание — через
   явный мост.
4. **Сверка с `decimal` — обязательная часть тестовой сюиты**, а не разовое
   упражнение.

## Состояние ветки

`main`, рабочее дерево чистое, всё смержено. Не пушилось — владелец об этом не
просил. Опубликованная версия — 1.1.2, тег `v1.1.2`.

## Здоровье кодовой базы

Проверено 2026-08-25 на Dart 3.13.0:

| Проверка | Результат |
|:--|:--|
| `dart test` | 258 проходят, 27 `skip` с номером дефекта |
| `dart format --output=none --set-exit-if-changed .` | чисто, 45 файлов |
| `dart analyze` | 1 замечание, ниже |

`example/lib/src/utils/output.dart:3:25` — `use_named_constants`, предложено
взять `Color256.gray12` вместо конструктора. Единственное замечание на весь
проект, в `lib/` чисто. К переработке отношения не имеет, владелец про него
знает.

## Известные пробелы

Полный разбор — в `docs/records/2026-08-25[3]-package-review.md`. Здесь только
то, что определяет ближайшие решения.

- **11 подтверждённых дефектов осталось**, все некритические и независимые
  друг от друга — Д5–Д15, разбор в ревью, воспроизводитель у каждого в
  `test/defects_test.dart`. Четыре критических (деление на отрицательное,
  переполнение `_pow10`, зависание на делении на ноль, потеря порядка в
  `toBigInt`) закрыты волной 1.
- **Сверка с `decimal` красная в трёх местах:** Д5 (`toStringAsFixed` не
  добивает нулями) и два про разбор строк — Д10 и Д12 (`tryParse` принимает
  шестнадцатеричное, `'-'` и табуляцию). Остальные расхождения, найденные
  ревью, закрыты волной 1. Полная картина по трём конкурентам — в ревью.
- **Заглавный API деления медленнее конкурента.** `1/3` через `operator /` плюс
  `try/catch` — 1971 нс против 1437 нс у `decimal`. Тотальный путь
  `divideToFraction().round(6)` даёт 565 нс, но спрятан в README.
- **Два семейства разошлись в 25 местах.** Аргумент про цену полиморфизма
  проверен измерением: общий интерфейс стоит 0 %, дорога только общая
  реализация через дженерики (2×).
- Покрытие `lib/` — 83.8 % строк (было 74.7 % до волны 0). Оба главных типа
  за 89 %, провал переехал в дроби и деление с остатком: `fraction.dart`
  66.2 %, `short_fraction.dart` 65.1 %, `division.dart` и
  `short_division.dart` по 62.5 %. Волна 2 требует не ниже 90 % по каждому
  файлу — закрывать придётся именно их.
- `toStringAsExponential`/`toStringAsPrecision` у `Decimal` бросают
  `UnimplementedError`, у `ShortDecimal` отсутствуют вовсе.
- Таблица бенчмарков в README устарела: `add` и `multiply` сейчас в пределах
  шума, а `divide-large` мы проигрываем.
- `example/README.md` — заглушка от `dart create`; реального примера
  использования в пакете нет.
