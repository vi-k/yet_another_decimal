# Текущее состояние

> Обновлено: 2026-08-31

Здесь только то, что происходит с проектом сейчас. Истории коммитов, разбора
законченных эпизодов и пожеланий владельца тут быть не должно — см. `git log`,
`docs/records/` и `docs/backlog.md`.

## Чем занимаемся

**Идёт работа по спеке на 128-битные промежутки в короткой семье** —
`docs/records/2026-08-31[4]-wide-int-design.md`. Суть: в четырёх местах
короткая семья берёт `BigInt` там, где хватило бы пары `int` на 128 бит.
Порядок работ, границы и доказательства неизменности результатов — в спеке.

**Волны 1–4 из пяти сделаны.** Первая — примитивы и тесты к ним,
`lib/src/short_decimal/wide.dart` и `test/short_decimal/wide_test.dart`.
Вторая — место 3, запасной путь `ShortFraction.compareTo`. Третья — место 2,
`_sumThatOverflowed`: `BigInt` ушёл совсем, `int.min + int.min` отвечается
явной проверкой в начале. Первые три холодные. Четвёртая — место 1,
`operator *`: приближение в `double` заменено точной проверкой на 128 битах,
`_productFits` и константа `_productThreshold` удалены, `_productOrNull` и
`toInt` зовут `productFits` из `wide.dart`. Ответы везде те же — весь
`dart test` зелёный и на 3.13.0, и на пороге 3.6.0.

**Место 1 замерено и принято** —
`docs/records/2026-08-31[5]-place-one-report.md`.
Чередующийся замер на тихой машине, база `851daad`
против `a69c42e`: `ShortDecimal` на `multiply-large-int` и `multiply-small-int`
даёт 0.050 → 0.046 мкс, это 1.087×, и цифры повторились в обоих раундах.
Контроль (`Decimal`, правка его не касается) дрейфовал в другую сторону на
1–3 %, так что настоящий выигрыш около 1.10×. Ухудшений нет.

Ожидание из спеки — «двигаться должны `dirty`, а `large-int` и `small-int`
живут под 2^31» — оказалось неверным, и это стоит помнить, берясь за место 4:
в цепочке `123000⁹` уже второе умножение даёт ~1.5·10¹⁰, быстрый путь из
четырёх сравнений там не срабатывает, и дальше всё идёт 128-битной дорогой.
А в `dirty` операнды перешагивают 2^31 только на последнем из трёх умножений,
и на фоне всей ячейки это тонет в округлении до трёх знаков. **Смотреть надо
не на имя ячейки, а на то, где в её цепочке операнды переваливают за 2^31.**

**Остаётся место 4** (`divide`) — самое большое, с тем же чередующимся
замером, но на тегах `divide` и `divide-and-view` (пятнадцать ячеек, около
13 минут). Рецепт — тот же, что отработал на месте 1:

```bash
cd example
# «до» — база, «после» — верхушка; и так дважды
git checkout <база> -- ../lib && dart compile exe bin/benchmark.dart -o bin/benchmark.exe
./bin/benchmark.exe divide denary
git checkout HEAD -- ../lib && dart compile exe bin/benchmark.dart -o bin/benchmark.exe
./bin/benchmark.exe divide denary
```

Пересборка обязательна после каждого `checkout`: стенд линкует пакет по пути.
В конце вернуть `lib/` на верхушку. Строки `Decimal` в выдаче — бесплатный
контроль: правка их не касается, и их дрейф показывает, сколько машина наврала
между прогонами. Скажет «хуже» — правка откатывается отдельным коммитом, и это
нормальный исход. Полный прогон стенда для таблиц README — отдельно и только
по явной просьбе владельца.

**Чему научила волна 1.** Шаг «вернуть назад» в делении Кнута срабатывает
примерно на одном делении из четырёхсот тысяч, и property-тест на 100 000
случаев его не видит вовсе — проверено мутацией — так что образцы для него
найдены отдельным перебором со счётчиком внутри функции и вписаны в тест
константами. Всякая правка `udivmod128by64` проверяется мутацией, а не
зелёным прогоном: зелёный прогон там ничего не значит.

1.2.0 вышла на pub.dev 2026-08-31. `main` ушёл вперёд `origin/main`: волны 1–4
и правки документации этого дня не отправлены. Отправить — решение владельца.

**Что помнить, берясь за производительность.** Таблицы README собраны из одного
прогона стенда целиком, поэтому любая правка, меняющая замеры, требует **нового
полного прогона** — час сорок пять монопольно занятой машины, и запускать его
можно **только по явной просьбе владельца**. Вынуть или подправить одну строку
таблицы нельзя.

Что уже разобрано, чтобы не ходить туда второй раз:

| Вопрос | Где ответ |
|:--|:--|
| Цена правок корректности короткой семье; сравнение с 1.1.2 | `2026-08-30[6]` |
| `divide-small` не догоняет 1.1.2 — семь вариантов, отрицательный результат | `2026-08-30[6]` |
| Размен `155a324`: умножение −20…−26 %, сложение +3–4 %; **принят владельцем** | `2026-08-30[6]`, `2026-08-31[1]` |
| Честный прогон: цена посторонней нагрузки — ровно 5 % на всех колонках | `2026-08-31[1]` |
| Чем проверялась документация и как это повторить | `2026-08-31[2]` |
| Цена места 1: 1.087× замерено, ~1.10× с поправкой на контроль | `2026-08-31[5]` |
| Как читать чередующийся замер и почему `Decimal` в нём контроль | `2026-08-31[5]` |

**В `docs/backlog.md` два пункта:** прототип адаптивного представления
`Decimal` (защита главного тезиса пакета — см. «Известные пробелы») и решение о
том, что делать с векторами `dectest`.

История разборов — в `docs/records/`, 57 записей. Шапка каждой говорит, что с
той работой сейчас: сделано, брошено или пересмотрено позже.

## Следующий шаг

**Осталось одно, и это не агентское дело:** пометить `yet_another_decimal` на
pub.dev как discontinued с указателем «replaced by denary». Делается в админке
пакета руками владельца.

Дальше по спеке — волна 5, место 4 (`divide`), рецепт замера выше.

## Что осталось незакрытым сознательно

- **`operator *` не проверяет сумму масштабов** (разбор в вердикте Р24). Самый
  горячий путь пакета; проверка здесь — два сравнения там, где у `ShortDecimal`
  вся операция стоит около трёх наносекунд. И проверять нечего: масштаб сам по
  себе безвреден, а степень десятки, которую он однажды потребует, ловится в
  `_pow10`.
- **Печать числа с абсурдным масштабом всё ещё съест память.**
  `(Decimal(1) >> 1000000000).toString()` строит строку в миллиард символов —
  степень десятки там не участвует, ловить нечего, а бросать из `toString()`
  нельзя (это Р25 наоборот). Арифметика, округление и сравнение такого числа
  теперь отвечают `ArgumentError`, печать — нет.
## Что есть под руками у любой правки в `lib/`

| Файл | Что делает |
|:--|:--|
| `test/support/reference.dart` | точная рациональная арифметика на `BigInt` и детерминированный генератор |
| `test/support/expect.dart` | хелперы проверки для обоих семейств |
| `test/property_test.dart` | 16 законов против модели вместо проверки отдельных значений |
| `test/competitors_test.dart` | сверка каждой операции с пакетом `decimal` плюс список осознанных расхождений |
| `test/defects_test.dart` | воспроизводители дефектов Д1–Д53, все зелёные, `skip` нет |
| `test/errors_test.dart` | типизированные отказы: типы, поля и совместимость с `ArgumentError` |
| `test/symmetry_test.dart` | наборы публичных членов двух семейств и два списка осознанных расхождений |
| `test/bridge_test.dart` | переходы между семействами |
| `test/decimal/`, `test/short_decimal/` | по файлу на тему, раскладка — в `docs/conventions.md` |

Стенд в `example/` — не примеры, а сравнение скорости; его методика описана в
`docs/architecture.md` и в `example/README.md`. Цифры для README берутся только
оттуда: AOT, `--passes=2`, по каждой ячейке лучшая из двух медиан.

## Решения, не подлежащие пересмотру вслепую

- **Красных тестов в `main` нет.** Воспроизводитель дефекта пишется с
  **корректным** ожиданием и помечается `skip: 'ДN: …'`; снятие `skip` — и
  есть критерий готовности правки.
- **Эталонная модель не использует код пакета**, иначе она повторяла бы его
  ошибки вместо того, чтобы их ловить.
- **Представление сверяется только там, где оно и есть предмет проверки** —
  `parse`, `toString`, `toStringAsFixed`, `normalized`.
- **Знак делителя выносится до разложения, а отрицается результат, не
  делимое:** у `ShortDecimal` отрицание делимого на `int.min` теряет знак.
- **`_align` в сравнениях не участвует** — там `_compare`, который не
  переполняется. У `~/`, `%`, `remainder`, `divideToFraction`,
  `divideToDouble` и `ShortDivision` стоит `_alignOrNull` с запасным точным
  путём на `BigInt`: **прежнее решение («там переполнение настоящее, а не
  промежуточное») опровергнуто ревью** — `2e19 ~/ 4` даёт представимые
  `5e18`, а выравнивание переполнялось. У `+` и `-` `_align` остался: при
  ненулевых операндах разрыв масштабов больше восемнадцати означает, что
  точный ответ и правда непредставим, а нулевой операнд теперь отсекается до
  выравнивания.
- **Переполнение чинится только там, где канонический результат помещается в
  int64.** Единственное исключение — округление `ShortFraction` к числу знаков,
  которое в int64 не влезает: там возвращается ближайшее представимое, потому
  что молчаливое переполнение в этом месте меняло знак (это и был Д11).
- **`precise_decimal` — главный конкурент; учимся у него качеству, сильнее
  остаёмся в своём.** Слово владельца от 2026-08-29. Наше своё — `ShortDecimal`
  (типа на int64 нет ни у кого), скорость на стенде и результат деления как
  значение; «точно или честный отказ» отличием быть перестало — у них то же
  самое. Под угрозой одна строка стенда, `add-dirty-int`, и защищает её
  адаптивное представление `Decimal`. Разбор — в
  `docs/records/2026-08-29[6]-positioning-direction.md`.
- **General Decimal Arithmetic целью соответствия не берём.** Контекст с
  `precision`, `NaN` с бесконечностями и сохранение показателя при печати —
  три несущих расхождения с моделью «точно или честный отказ»; ближайший
  родственник пакета — `BigDecimal` без `MathContext`, а не GDA. Разбор и то,
  что из спеки взять стоит, — `docs/records/2026-08-29[4]-gda-scope-design.md`
  (2026-08-29). Три вытекающих пункта владелец положил в `docs/backlog.md`.
- **`pow` с отрицательным показателем не переполняется молча.** Степень под
  единицей строится с проверкой, а не `math.pow`: делить на завёрнутую степень
  значит отвечать за число, которого никто не просил — `int.min^-3` жаловался
  на деление на ноль. Где степень из int64 вышла, а обратная величина
  основания есть, ответ считается от неё (`5^-30` — это `0.2^30`); где ответа
  в int64 нет вовсе — `UnsupportedError`, тот же отказ и тот же довод, что у
  `inverse`. Принято 2026-08-29 по правилу Д27, разбор — `2026-08-29[2]`.
- **Экспоненциальная запись не строит степень десятки.** Ведущие цифры
  округляются строкой (`roundDigits` в `helpers.dart`), поэтому `1e-1000000`
  печатается, а не отвергается, и число в пятьдесят тысяч цифр не платит за
  показ трёх. `toStringAsPrecision` пишет число целиком и границу миллиона
  знаков держит — но проверяет её до вычисления числа знаков и называет в
  отказе `precision`.
- **`isDivisibleBy` отвечает про конечную запись, а не про целое частное.**
- **Проверка переполнения умножения вписана в оператор, а не вынесена в
  метод:** вынесенная не инлайнится, 0.089 против 0.065 µs.
- **Штатный `BigInt.gcd` отвергнут замером.** Взят только `int`-путь.
- **Порог SDK — `^3.6.0`, и он держится не на слово.** Весь `dart test`
  прогоняется на Dart 3.6.0 из `~/fvm/versions/3.27.0`. Нижние границы
  зависимостей выбираются так, чтобы в них попадало то, что Flutter пинит из
  своего SDK; правило и таблица версий — в `docs/conventions.md`.
- **Публичные члены пакета обязаны иметь dartdoc:** правило
  `public_member_api_docs` включено в корневом `analysis_options.yaml`. В
  `example/` оно выключено — стенд не библиотека.
- **Степень десятки ограничена миллионом, и проверок две.** Константа одна на
  пакет — `maxDecimalExponent` в `helpers.dart`, та же, что ограничивает
  показатель при разборе строки. Первая проверка на входе, у всего, что берёт
  число знаков у вызывающего (округления обоих семейств и обоих типов дробей,
  `divide` с числом знаков, три метода печати): она даёт понятную ошибку с
  именем аргумента. Вторая — в `Decimal._pow10`, единственном месте, где
  степень строится: она ловит показатель, пришедший из масштаба или из разрыва
  масштабов, и стоит на редком пути мимо кеша, то есть бесплатна.

  **Границу нельзя ставить на масштаб.** Пробовал 2026-08-27 — ограничил
  миллионом сдвиги, `pow` и `movePoint`, и сломал мост: масштаб больше
  миллиона получается обычным умножением, а мост внутри сдвигает, так что
  `toShortDecimalOrNull` вместо `null` и `toDecimal` вместо точного ответа
  начали бросать. Откачено в тот же заход, на регрессию есть тест в
  `test/bridge_test.dart`.
- **Стороны дроби читаются своим сканером, `scanInteger`:** знак и цифры, и
  ничего больше. Пробелы вокруг принимаются — как у чисел. Показателя нет
  намеренно: у числа он уходит в масштаб, а у дроби его пришлось бы
  материализовать в int64, то есть молча переполнить.
- **`ShortDecimal.divide` умеет округлять без дроби.** Когда `ShortFraction`
  из пары не строится — делитель 2^63, знаменателю негде быть положительным, —
  округление идёт по паре в `BigInt`. `divideToFraction` при этом честно
  бросает: такой дроби в int64 нет, и это записано в его dartdoc.
- **Публичные конструкторы проверяют аргументы броском, а не ассертом.**
  Ассерта нет в release, и `Decimal(1, shiftRight: -3)` там молча давал 1000.
  Внутренние инварианты (`_pow10`, `_pow5`, приватные конструкторы дробей)
  остаются на ассертах: там проверяется пакет, а не пользователь.
- **Кеш печати для `ShortDecimal` отвергнут замером.** `Expando` с прагмой
  совместим, но запись в него стоит 84.5 нс против 1.1 нс у поля, а первая
  печать — это всегда запись: `raw-view` замедлялся втрое при том, что
  `repeat-view` ускорялся в одиннадцать раз. Разбор — `2026-08-28[6]`.
  Пересматривать только с новым замером, а не по памяти.
- **Три ступени снятия нулей и строковый кеш — только у `Decimal`.** Три
  ступени у `ShortDecimal` замерены и медленнее. Кеш там запрещён прагмой
  `vm:deeply-immutable`, а не `const`-конструкторами: она допускает только поля
  `final` и не `late`, и отказ от `const` кеша бы не дал. Разбор и цифры —
  `docs/records/2026-08-28[3]`.
- **Ожидаемые ответы стенда считаются вне Dart.** Точной рациональной
  арифметикой, и выписываются в код константами. Иначе стенд мерит скорость
  неправильных вычислений — именно эта сверка поймала `compareTo` у `fixed`.
- **Цифры для README снимаются с `--passes=2`, в ячейку идёт лучший проход.**
  Медиана пяти прогонов не спасает от всплеска нагрузки, который длится дольше
  всей серии одного бенчмарка; помеха может замер только замедлить. При
  последнем перемере два прохода разошлись на 15 ячейках из 190.

- **Взаимная простота делителя с десяткой — это ответ, а не подсказка.** Если
  делитель не содержит ни двойки, ни пятёрки, то `d | n·10^k` равносильно
  `d | n`, а остаток от `n·10^k / d` округление и так считает. Поэтому `divide`
  с числом знаков на таком делителе не задаёт отдельного вопроса о
  представимости: ненулевой остаток округления сам доказывает, что конечной
  записи нет. `divideOrNull` зовётся только при нулевом остатке — на точном
  делении, где он и даёт правильное представление, не добитое нулями до числа
  знаков. Тем же доводом из `divideOrNull` убран `gcd`. Быстрый путь уступает
  дорогу вне границы `maxDecimalExponent`, чтобы ошибка приходила оттуда же,
  откуда приходила, и называла аргумент.
- **Масштабы и число знаков сворачиваются в одну степень десятки.** Так у
  обоих семейств. Выравнивать пару в дробь и домножать её ещё раз — значит
  умножать обе стороны на масштаб операндов; у `ShortDecimal` это выводило
  вычисление из int64 на значениях, которые в int64 умещаются, и роняло строку
  в двадцать раз. Целочисленная ветка берётся только при положительном
  делителе: `-int.min` не снимается.
- **Выравнивание вписано в `+` и `-`, а не взято из `_align`.** Кортеж,
  который тот возвращает, — объект, и его выделение видно на операции в
  двадцать шесть наносекунд. Опыт с `@pragma('vm:prefer-inline')` показал, что
  дело именно в кортеже: с прагмой путь равных масштабов остался на 0.1494, без
  кортежа — 0.1274. В остальных девяти местах `_align` остаётся: там он не на
  горячем пути.
- **`raw-view` меряет первую печать, и пул это обеспечивает.** Свежего объекта
  мало: кеш, ключуемый значением, ответит и ему. Набор — пул в
  `viewPoolCycles` = 103 цикла, обходится по порядку, и он больше любого кеша в
  сравнении. Сверяется **весь** пул, а не первый цикл; эталон — `plainString`
  в `example/lib/src/tests.dart`, он не заимствует кода ни у одного из
  сравниваемых пакетов.

## Решения владельца, действующие сейчас

Обоснование каждого — в `docs/records/2026-08-25[4]-package-rework-design.md`.

1. **Две ступени: 1.2.0, затем 2.0.** Все пятнадцать дефектов починены без
   слома API, недостающее добавлено аддитивно. Ломающее — отдельно в 2.0.
2. **Переполнение `ShortDecimal` остаётся молчаливым.**
3. **Общий интерфейс `FixedPoint` плюс тест симметрии**, реализации остаются
   раздельными. В 1.2.0 интерфейс внутренний, не экспортируется. Межсемейных
   операций не будет: смешивание — через явный мост.
4. **Сверка с `decimal` — обязательная часть тестовой сюиты.**

Принято 2026-08-26, разбор — в `docs/records/2026-08-26[13]`, решения — в
`[14]`:

5. **`operator /` не трогаем.** Он остаётся быстрой формой, которая бросает.
   То, ради чего его хотели сломать, закрыто в 1.2.0 тотальными формами.
6. **`Decimal` неизменяемым не делаем.** `vm:deeply-immutable` для класса с
   полем `BigInt` невозможен (проверено компилятором), а мутирующий
   `optimize()` вылечен `normalized()` — сначала устареванием, а при
   переименовании в `denary` и удалением.
7. **`FixedPoint` остаётся внутренним.** Публичный супертип — обязательство
   навсегда, а межсемейных операций всё равно не будет.
8. **Представление открывается от канонической формы**, не от сырой.
9. **Наших `toDecimal()` на `int` и `BigInt` больше нет.** Они конфликтовали
   с одноимёнными из `decimal`: пока оба существовали, `5.toDecimal()` был
   неоднозначен даже для того, кто звал чужой. В 1.2.0 были помечены
   устаревшими, а при переименовании в `denary` удалены — переносить в новый
   пакет то, чем в нём никто не пользовался, незачем (2026-08-28).

Принято 2026-08-27:

10. **Пакет называется `denary`.** Решено 2026-08-27, подтверждено
    2026-08-28 против альтернативы `fast_decimal` (и против переименования
    `ShortDecimal` в `FastDecimal`): имя-сравнение проверяется каждым чужим
    релизом, а `precise_decimal` уже забрал у нас одну строку. **Сделано
    2026-08-28**, план — `docs/records/2026-08-28[4]`, отчёт — `[5]`. Момент
    выбран до публикации 1.2.0: под старым именем остался один релиз 1.1.2, а
    `CHANGELOG` 1.2.0 выходит уже под новым. Цена решения измерена: у
    `yet_another_decimal` на pub.dev было 20 загрузок за 30 дней и 2 лайка при
    160/160, терять нечего. Старый пакет пойдёт в discontinued с указателем
    «replaced by» — после публикации, руками владельца.

    Имя выбрано так: «точный» и «precise» — свойство, общее у нас с `decimal`,
    `big_decimal` и `precise_decimal`, а `exact_decimal` вдобавок неотличим от
    последнего по смыслу; «быстрый» — сравнение, которое портится от чужого
    релиза. `denary` — английское слово для основания десять, не утверждение,
    портиться в нём нечему, и три точки входа читаются как пространство имён:
    `denary/decimal.dart`, `denary/short_decimal.dart`. Цена — редкое слово,
    в поиске по «decimal» имя само не всплывёт, выдача ляжет на описание и
    topics.

    **На замеры переименование не влияет:** нашего имени нет в блоке окружения
    README, колонки таблиц называются `Decimal` и `ShortDecimal`. Прогон можно
    ставить до или после.

11. **У README есть русский перевод — `README.ru.md` в корне.** В пакет он не
    идёт (`.pubignore`), а актуальным обязан быть всегда: правка `README.md`
    не закончена, пока то же самое не перенесено в перевод, и это один коммит,
    а не два. Как переносить — в `docs/conventions.md`, раздел «README и его
    перевод».

## Состояние ветки

`main`, рабочее дерево чистое, всё запушено: `main` = `origin/main`. Порог SDK
опущен до `^3.6.0`, проект переформатирован в стиль этой языковой версии.

`origin` — `https://github.com/vi-k/denary.git`: владелец переименовал
репозиторий 2026-08-28, адрес переставлен в тот же заход. Прежние адреса
(`vi-k/decimal2`, затем `vi-k/yet_another_decimal`) работают через редирект
GitHub, но в дереве и в `pubspec.yaml` стоит новый.

Опубликованная на pub.dev версия — **1.2.0**, тег `v1.2.0`, коммит `65f301e`.
Под старым именем `yet_another_decimal` осталась 1.1.2; пометить тот пакет
discontinued владельцу предстоит руками.

## Здоровье кодовой базы

Проверено 2026-08-31 на Dart 3.13.0:

| Проверка | Результат |
|:--|:--|
| `dart test` | 834 проходят, `skip` нет; на Dart 3.6.0 перепроверено 2026-08-30 |
| `dart format --output=none --set-exit-if-changed .` | чисто, 56 файлов |
| `dart analyze` | чисто, и в корне, и в `example/` |
| Покрытие `lib/` | 1640 из 1640 строк, 100 %, мерено 2026-08-30 |
| `dart pub publish --dry-run` | 0 предупреждений, архив 171 КБ |
| Примеры документации | 33 из README и 57 из dartdoc выполнены и сверены со своими комментариями, 2026-08-31 (`2026-08-31[2]`) |
| release-сборка | владелец проверил 2026-08-27: работает |

**`pana` перед публикацией не перемерен.** Последняя цифра — 160/160 от
2026-08-28, снятая до трёх добавлений в API и до всей волны 29–31 августа.
Перемерять локально незачем: pub.dev считает свой score сам в течение часа
после публикации, и смотреть надо именно его — на странице пакета.

Покрытие после правок 28 августа проседало до 99.54 % — запасная дорога в
`ShortDecimal._roundedQuotient` (показатель вне миллиона) осталась без тестов,
потому что раньше через неё шло всё. Закрыто двумя тестами; рецепт замера — в
`docs/conventions.md`.

Про release отдельной строкой, потому что это единственная проверка, которую
`dart test` не делает: в release нет ассертов, и там видно только то, что
брошено по-настоящему. Публичные конструкторы и границы степени десятки на
бросках, а не на ассертах, — именно ради этого случая.

Всё это гоняется и на CI: `.github/workflows/ci.yml`, две работы — пакет на
3.6.0 и на стабильной, стенд отдельно. Зелено на всех трёх последних коммитах,
включая `Release 1.2.0`.

## Известные пробелы

- **`operator /` бросает, и так и останется** — решение владельца, а не
  недоделка: обоснование слома закрыто аддитивно. Тотальные формы есть,
  `divideOrNull` вчетверо быстрее ловли исключения, README подаёт первичной
  именно её.
- **Осознанных расхождения с `decimal` два** — `operator /` бросает вместо
  возврата дроби, и `'1.'` мы за число не считаем, а конкурент считает.
  Расхождения семейств перечислены в `test/symmetry_test.dart` двумя списками.
- **`add-dirty-int` — единственная строка, где мы вторые.** 0.223 против 0.167
  у `precise_decimal` по честному прогону 2026-08-31; разрыв против замера
  28.08 (0.225 против 0.170) не вырос — правки корректности его не тронули.
  Причина найдена и правкой сложения не лечится: у них
  несущее значение хранит `int`, пока тот вмещает, и выравнивание малых
  значений идёт целыми числами. Это `intCompact` из Java. Выиграть можно только
  сменой представления `Decimal` — и это же разговор о том, зачем тогда
  отдельный `ShortDecimal`. Ломающей такая смена, похоже, не будет (см. ниже),
  но проверяется она прототипом и замером, а не рассуждением. Разбор —
  `docs/records/2026-08-28[1]`, расстановка сил — `2026-08-29[6]`, пункт
  положен в `docs/backlog.md`.

- **2.0 не существует.** Список, отложенный спекой, разобран 2026-08-28
  (`docs/records/2026-08-28[7]`): из шести пунктов пять закрыты в 1.2.0, шестой
  решено не делать, а три остававшихся устаревших члена — `optimize()` и два
  расширения `toDecimal()` — удалены в тот же день, при переименовании. В
  `denary` ими никто не пользовался, а переход с `yet_another_decimal` и так
  правится руками: лучшего момента снять устаревшее не будет. **Ломать больше
  нечего, и повода для мажора нет.**

  **Единственный кандидат в такой повод — адаптивное представление `Decimal`**
  (хранить `int`, пока влезает). И он, похоже, мажора не требует: публичные
  типы не меняются, ломается только `base`, а он `@visibleForTesting`.
  Гипотеза, проверяется прототипом и замером.
