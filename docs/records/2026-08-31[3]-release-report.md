> **Состояние на 2026-08-31:** сделано целиком. 1.2.0 опубликована на pub.dev,
> и последнее, что оставалось владельцу, — `yet_another_decimal` помечен
> discontinued — тоже сделано в тот же день.
> **Что это:** отчёт о публикации — чем связка была и что оставалось владельцу.
> **Связанные записи:** `2026-08-31[1]` (честный прогон, чьи числа ушли в
> README релиза), `2026-08-31[2]` (сплошная проверка документации перед ним),
> `2026-08-28[4]` и `[5]` (переименование в `denary`).

# Публикация 1.2.0

## Связка

Прошла целиком и подряд, как требует `AGENTS.md`, по прямой просьбе владельца,
состоявшей только из неё:

```bash
git commit --allow-empty -m 'Release 1.2.0'   # 65f301e
git tag v1.2.0
git push origin main
git push origin v1.2.0
dart pub publish --force
```

**Коммит пустой намеренно.** Версия в `pubspec.yaml` и запись в `CHANGELOG.md`
были готовы и закоммичены раньше, менять к моменту публикации было нечего, но
связка требует своего коммита — и тегу нужен якорь, который называет себя
релизом, а не правкой документации. Прошлые релизы тегировались на
содержательный коммит (`v1.1.2` → «Change version»), потому что там правка
версии как раз и приходилась на момент публикации.

Ответ сервера: `Successfully uploaded https://pub.dev/packages/denary version
1.2.0`. Страница обновляется до десяти минут.

## Что ушло в пакет

Архив 171 КБ, `dart pub publish --dry-run` перед этим — **0 предупреждений**.

- **Идут:** `lib/`, `test/`, `example/` целиком, `README.md`, `CHANGELOG.md`,
  `LICENSE`, `analysis_options.yaml`.
- **Не идут:** `README.ru.md`, `docs/`, `AGENTS.md`, `CLAUDE.md` — по
  `.pubignore`.

Состояние на момент публикации: `dart analyze` чисто, `dart test` — all tests
passed, `dart format` — без изменений, числа в README сняты честным прогоном
2026-08-31 на проверенной машине.

## Что оставалось владельцу — сделано

Пометить `yet_another_decimal` на pub.dev как **discontinued** с указателем
«replaced by denary». Это делается в админке пакета и агенту недоступно. Под
старым именем там остаётся 1.1.2.

**Сделано владельцем 2026-08-31.** Связка релиза закрыта полностью, переезд с
прежнего имени закончен.
