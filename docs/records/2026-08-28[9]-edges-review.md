> **Состояние на 2026-08-29:** закрыто целиком. Решения владельца приняты
> 2026-08-29, пятнадцать находок починены в тот же день, три последние — Д34,
> Д37 и Д35 — перечитаны и починены следом: разбор в
> `2026-08-29[2]-edges-tail-plan.md`, отчёт в `2026-08-29[3]-edges-tail-report.md`.
> **Что это:** сведённые вместе два независимых ревью кода (Fable-агент и
> Codex) и разбор того, что они нашли: семнадцать мест, из них одиннадцать
> проверены лично.
> **Связанные записи:** `2026-08-28[8]-user-view-report.md` (обзор глазами
> пользователя, Д16–Д19), `2026-08-25[3]-package-review.md` (нумерация
> дефектов продолжает ту), `2026-08-29[2]` и `2026-08-29[3]` (хвост: Д34,
> Д37, Д35).

# Ревью краёв: масштаб, канонизация, округление

## Как смотрели

Два независимых прохода по одному и тому же коду, каждый со своей песочницей
и требованием подтверждать находку запуском (номеров восемнадцать: Д20–Д37):

- **Fable-агент** — прочитал все 4590 строк `lib/`, прогнал около 700 тысяч
  сверок: дифференциальные сетки двух семейств, инварианты деления, брутфорс
  против эталонов, раунд-трипы печати.
- **Codex** — 30 минут, воспроизводители через отдельную VM (песочница не
  дала создать проект, и он обошёлся `/dev/stdin`).

**Пересеклись они всего в двух точках** — двойное округление и дыра в быстром
пути `divide`. Остальное каждый нашёл своё, и это главный довод в пользу того,
чтобы гонять два прохода, а не один.

## Корень: масштаб — обычный `int`, и это течёт

Одиннадцать из семнадцати находок — одна и та же болезнь. Масштаб хранится в
`int`, арифметика над ним проверяется **не везде**, а канонизация умеет его
менять. У сдвигов проверка есть с самого начала, у деления появилась вчера
(Д19) — и вчерашняя правка, как выяснилось, и неполна, и стоит не в том месте.

| № | Что происходит | Кто нашёл | Проверено мной |
|:--|:--|:--|:--|
| Д20 | `normalized()` меняет значение: `base 10, scale −2^63` становится `base 1, scale 2^63−1` — огромное число превращается в исчезающе малое | Codex | да |
| Д21 | `ShortDecimal(100, shiftLeft: 2^63−1)` даёт то же самое прямо в конструкторе; `exponent` теряет знак на `−int.min` | Codex | да |
| Д22 | факторизация делителя (`scale += twos/fives`) заворачивает масштаб в обеих семьях: `(1 >> max).divideOrNull(2)` даёт `scale −2^63` | Codex | нет |
| Д23 | быстрый путь `divide` считает показатель до проверки: `divide(…, scaleOnInfinitePrecision: 2)` отвечает `0.23`, пока три соседние формы бросают | оба | да |
| Д24 | проверка Д19 смотрит на сырые масштабы: `raw == canonical`, но деление одного бросает, а другого — работает. **Тест `defects_test.dart` закрепил это неверное ожидание** | Codex | да |
| Д25 | отрицательный `pow` возводит в положительную степень раньше, чем обращает: равные значения дают разные исходы, а `5^-28` не считается, хотя представим | Codex | нет |

## Второй корень: край int64 у `ShortDecimal`

Политика «переполнение молчаливое» заявлена и принята. Но эти случаи не
переполнение, а потеря того, что помещается:

| № | Что происходит | Кто нашёл | Проверено мной |
|:--|:--|:--|:--|
| Д26 | `ShortDecimal(max) + ShortDecimal(3)` даёт отрицательное, хотя точный ответ `9223372036854775810` в канонической форме умещается (`base 922337203685477581, scale −1`). Для умножения это чинили как Д13, для сложения — нет | Codex | да |
| Д27 | конечное частное, не влезающее в int64, возвращается **с перевёрнутым знаком**: `1 / 2^62` даёт `−0.0000…8536…` вместо `+0.0000…2168…`, и `isDivisibleBy` при этом отвечает `true`. `divide` с явным числом знаков наследует ту же ложь | Fable | да |
| Д28 | `toInt()` за границей int64 отвечает тремя разными способами: `Decimal` клампит, `ShortDecimal.toInt` заворачивает, `~/` того же `ShortDecimal` клампит. `−1e19` даёт **положительное** число | Fable | да |

## Третий корень: двойное округление

| № | Что происходит | Кто нашёл | Проверено мной |
|:--|:--|:--|:--|
| Д29 | когда результат не влезает в int64, цифры сбрасываются **по одной**, и `round`/`roundToEven` округляют на каждом шаге: `.85365 → .85 → .8` вместо `.9`. `floor`, `ceil` и `truncate` при этом точны — они идемпотентны, а «к ближайшему» нет | оба | да |

Это прямое продолжение вчерашней правки Д17: направление я сохранил, а
sticky-информацию — нет. Лечится однократным округлением: разделить на
`10^k` за один раз и решить по остатку, а не крутить цикл.

## Четвёртый корень: где проходит граница дозволенного

Здесь нужен не патч, а решение. Сейчас граница миллиона стоит на **числе
знаков, которое просит вызывающий**, и на степени десятки в `_pow10`, но не на
масштабе и не на длине разбираемой строки:

| № | Что происходит | Кто нашёл | Проверено мной |
|:--|:--|:--|:--|
| Д30 | `Decimal.parse('1e1000000')` и `1e-1000000` — законные значения, но `==`, `compareTo`, `+`, `%`, `~/` на этой паре бросают. `ShortDecimal` на тех же значениях отвечает | Fable | да |
| Д31 | `parse` принимает строку с двумя миллионами дробных знаков, после чего `truncate()` и `toInt()` бросают — хотя ответ очевидно `0`. `ShortDecimal` отвечает `0` | Fable | да |

## Мелочи

| № | Что происходит | Кто нашёл | Проверено мной |
|:--|:--|:--|:--|
| Д32 | `print(e)` на исключении деления сам бросает `ArgumentError` — в обеих семьях. Исключение, которое нельзя показать в логе, а его dartdoc обещает «переспросить, а не тупик» | Fable | да |
| Д33 | `ShortDecimal.inverse` бросает `ArgumentError` там, где dartdoc обещает `UnsupportedError` | Fable | нет |
| Д34 | `ShortFraction.operator /` отказывает на представимом результате, а `*` в той же точке молча теряет знак | Fable | нет |
| Д35 | `toStringAsExponential`/`toStringAsPrecision` бросают, называя аргумент со значением, которого вызывающий не передавал | Fable | нет |
| Д36 | знак нуля в `divideToDouble` зависит от семьи и от внутреннего пути: `−0.0` против `0.0` | Fable | нет |
| Д37 | `ShortDecimal(int.min).pow(-3)` бросает «division by zero» на ненулевом основании | Fable | нет |

## Решения владельца от 2026-08-29

| Вопрос | Решение |
|:--|:--|
| `toInt()` за границей int64 | **клампить** — как `BigInt.toInt`, `double.toInt` и как `~/` этой же семьи. Конверсия не наследует политику молчаливого переполнения |
| предел у разбираемой строки | **ограничить дробные знаки** тем же миллионом, что и показатель: одно число читается одинаково, как его ни запиши |
| граница на масштаб | **проверять при рождении** — там, где масштаб вычисляется, а не в каждой операции поверх |
| Д27, непредставимое частное | **`null`** — `divideOrNull` обещает его там, где ответа нет; выдуманное число с чужим знаком не переполнение значения |

Ниже — вопросы в том виде, в каком они ставились.

## Что решать владельцу

Три вопроса, без ответов на которые правки будут гадательными.

1. **Что делает `toInt()` за границей int64** — заворачивает (как арифметика
   `ShortDecimal`) или клампит (как `BigInt.toInt`)? Сейчас в одной семье оба
   ответа сразу, и это единственное, что точно неверно.
2. **Где предел у разбираемой строки.** Либо `parse` отказывается читать то,
   с чем потом не сможет работать, либо операции обязаны отвечать на всё, что
   `parse` принял. Сейчас ни то ни другое.
3. **Граница на масштаб.** Ставить её пробовали 2026-08-27 и откатили — сломала
   мост. Но без неё каждая операция обязана проверять масштаб сама, и шесть
   мест уже показали, что так не выходит. Возможен третий путь: не запрещать
   масштаб, а проверять его в одной точке — там, где он рождается
   (`_asIs`/`_pack`), а не в каждой операции.

## Что чинится без вопросов

Д23, Д24, Д27, Д29, Д32 — баги без развилок: неверный ответ, неверный тест,
непечатаемое исключение. Д29 и Д24 — прямое следствие вчерашних правок, и
чинить их надо первыми.

## Что уже починено

Восемь мест из восемнадцати — те, что не упирались в решения:

| № | Что сделано |
|:--|:--|
| Д29 | округление считается от исходной пары: масштаб, не влезающий в int64, снимается с делителя, и деление повторяется целиком. Двойного округления больше нет ни в одном режиме |
| Д24 | при переполнении сырых масштабов решают канонические формы: равные значения делятся одинаково. **Мой вчерашний тест закреплял обратное и переписан** |
| Д23 | быстрый путь `divide` заходит туда только когда все три масштаба внутри миллиона — три таких числа не заворачиваются. В обоих семействах |
| Д26 | сложение и вычитание считают точный ответ, когда его каноническая форма помещается: `max + 3` даёт `922337203685477581 × 10⁻¹`. Тот же довод, что у умножения (Д13); сумма, которой канонизация не помогает, по-прежнему переполняется молча |
| Д32 | исключение деления печатается: сообщение собирается по частям, и на месте того, чего в этой семье нет, так и написано |
| Д25 | `pow` считает от канонической формы: равные значения отвечают одинаково, как и в Д24 |
| Д33 | `inverse` бросает обещанный `UnsupportedError`. Вторая дверь нашлась рядом: ветка отрицательного масштаба проваливалась в тот же чужой бросок |
| Д36 | знак нуля согласован: `0 / −5` даёт `−0.0` в обеих семьях, как обычное деление в Dart. Дробь его не несёт — `0/−5` сокращается в `0/1`, — поэтому знак восстанавливается на месте деления |
| Д34 | знак делителя выносится в числитель до построения произведений: 956 представимых ответов, которые деление дробей отвергало, теперь считаются. Вторая половина находки — молчаливая потеря знака у `*` — политика, а не дефект (2026-08-29) |
| Д37 | степень под единицей строится с проверкой переполнения; где степень из int64 вышла, ответ считается от обратной величины основания (`5^-30` — это `0.2^30`). «Division by zero» на ненулевом основании больше нет, выдуманный делитель из текста исключения ушёл (2026-08-29) |
| Д35 | ведущие цифры округляются строкой: `toStringAsExponential` не строит степень десятки вовсе и печатает `1e-1000000`, а отказ `toStringAsPrecision` называет `precision`, а не завёрнутое число знаков (2026-08-29) |

### Цена решений 2026-08-29

Замер чередованием, дважды, после всех правок:

| | база | стало | |
|:--|--:|--:|:--|
| `Decimal`, всё | | | в пределах шума |
| `ShortDecimal` сложение | 0.145 | 0.154 | медленнее на 6 % |
| `ShortDecimal` `divide-dirty-int` | 0.022 | 0.024 | медленнее на 9 % |
| `ShortDecimal` `divide-small-int` | 0.109 | 0.124 | медленнее на 14 % |
| `ShortDecimal` округление и печать | | | без разницы |

Старшее семейство не заметило ничего: там проверки стоят на путях, где и так
работает `BigInt`. Короткое платит — и платит за то, что деление перестало
выдумывать числа, а сложение перестало терять представимый ответ.

По дороге замер поймал неудачную первую попытку: проверка через `_productFits`
стоила `divide-small-int` **21 %**, потому что множит `double` и иногда делит.
Вопрос «влезет ли `base × 5^n`» решается сравнением с числом, посчитанным
заранее, — две таблицы порогов, и осталось 14 %.

Прецедент, по которому это принимается: за корректность умножения в Д13
заплатили 0.065 → 0.089 µs, то есть 37 %.

### Цена правок 2026-08-28

Замер чередованием, дважды. Всё, кроме сложения, — в пределах шума.
Сложение у `ShortDecimal` подорожало на 6 %: 0.136 → 0.144 µs. Вписывание
проверки в оператор (приём, который сегодня дважды снимал такую надбавку)
здесь не помогло — платит сама проверка, а не вызов. Это в рамках прецедента:
за корректность умножения в Д13 заплатили 0.065 → 0.089 µs, то есть дороже.

### Чего не трогал и почему

*Написано 2026-08-28, до решений владельца. Все три случая закрыты 2026-08-29,
разбор — в `2026-08-29[2]`.*

- **Д34 и Д37** — точный ответ там непредставим, и это та же политика
  молчаливого переполнения, что в Д27. Без решения владельца трогать нельзя.
  **Оказалось не так:** брутфорс нашёл 956 пар, где ответ представим, а его
  отвергают, и `5^-30` из той же породы.
- **Д22** — переполнение масштаба, то есть третий вопрос.
- **Д35** — честная починка означает печатать экспоненциальную запись, вовсе
  не строя степень десятки; это тоже разговор о границе. Заплатка на текст
  сообщения четырёх правок не стоит. **Сделана именно честная починка**, и
  разговора о границе она не потребовала.

### Почему Д27 вернулся под `skip`

Я отнёс его к бесспорным и ошибся. Правка отменяла решение, записанное в
`test/short_decimal/divide_test.dart`: «переполнение остаётся молчаливым — но
не падением». Существующий тест её и отверг. Откачено; вопрос — к владельцу,
вместе с тремя прежними. Спор в том, что перевёрнутый знак — не переполнение
значения, а выдуманное число, и `divideOrNull` обещает `null` там, где ответа
нет.

## Чего это ревью не покрыло

- **JS-платформу.** Оба прохода гонялись только на VM: там `int` не int64,
  и таблицы степеней с границей 2^53 ведут себя иначе.
- **Печать чисел с масштабом порядка 10^9** — упирается в минуты работы
  `BigInt.toString`, оба прохода это отметили и обошли.
